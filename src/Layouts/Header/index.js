import { Divider } from '@mui/material';

const HEADER = (props) => {
  return (
    <Divider className="divider">
      Arabic number conversion tool by Peter Berger
    </Divider>
  );
};

export default HEADER;
